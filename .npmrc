# Faster installs (large lockfile ~1.4k packages). Security: npm run security:deps / guard:all / CI.
audit=false
fund=false
# prefer-offline can stall or retry oddly when the cache is partial; default fetch is more reliable.
# More parallel tarball fetches per host (default 15).
maxsockets=32
